{
  "name": "x-arcade",
  "version": "0.1.0",
  "description": "Terminal-native daily mini-games for the dead minutes while your coding agent works.",
  "type": "module",
  "bin": {
    "arcade": "./dist/index.js",
    "x-arcade": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b && chmod +x dist/index.js",
    "//build": "tsc emits index.js without the executable bit, so the `arcade` bin stops working after every clean rebuild. npm sets it on install for published packages, but never for a local npm link."
  },
  "dependencies": {
    "@x-arcade/shared": "*"
  },
  "keywords": [
    "tui",
    "terminal",
    "game",
    "snake",
    "cli"
  ],
  "license": "MIT"
}
